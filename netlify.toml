# Netlify-Konfiguration für den statischen Export der neuen CloudOptima-Website.
# Publish-Ordner ist der Next.js-Export "out/"; es laufen keine Functions.
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
