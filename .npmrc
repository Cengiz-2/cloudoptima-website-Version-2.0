# Falls Netlify die Abhängigkeiten mit pnpm installiert (Alt-Einstellung der
# Site), ein flaches, gehoistetes node_modules wie bei npm erzeugen. Verhindert
# Build-Fehler durch pnpms strikte Isolation. npm ignoriert diese Option.
shamefully-hoist=true
